import PlatformLayout from '@/components/ui/PlatformLayout';

export default function MentorsDirectoryLayout({ children }: { children: React.ReactNode }) {
  return <PlatformLayout>{children}</PlatformLayout>;
}
